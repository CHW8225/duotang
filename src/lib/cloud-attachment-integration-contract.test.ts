import { readFileSync } from "node:fs";import { describe,expect,it } from "vitest";
describe("真实PG+COS可选验收",()=>{it("凭据缺失明确SKIP并覆盖完整云端场景",()=>{const source=readFileSync("scripts/verify-cloud-attachments.ts","utf8");expect(source).toMatch(/SKIP cloud attachment integration/);expect(source).toMatch(/getBucketAcl/);expect(source).toMatch(/Promise\.all\(uploads\)/);expect(source).toMatch(/eleventh/);expect(source).toMatch(/softDeleteRecord/);expect(source).toMatch(/restoreRecord/);expect(source).toMatch(/compensationStore/);expect(source).toMatch(/cos_cleanup_jobs/);expect(source).toMatch(/processCosCleanupJobs/)});it("package提供显式命令",()=>{const pkg=JSON.parse(readFileSync("package.json","utf8"));expect(pkg.scripts["test:cloud:attachments"]).toBe("tsx scripts/verify-cloud-attachments.ts")})});
