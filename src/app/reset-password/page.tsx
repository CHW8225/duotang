import { AuthMessage,AuthPage } from "@/components/AuthPage"; import { resetPasswordAction } from "../auth-actions";
export default async function ResetPage({searchParams}:{searchParams:Promise<{token?:string;error?:string}>}){const q=await searchParams;return <AuthPage title="设置新密码" description="新密码至少12个字符，修改后其他登录会话会立即失效。"><AuthMessage error={q.error}/><form action={resetPasswordAction} className="auth-form"><input type="hidden" name="token" value={q.token??""}/><label>新密码<input name="password" type="password" minLength={12} required autoComplete="new-password"/></label><button type="submit" disabled={!q.token}>重置密码</button></form></AuthPage>}
