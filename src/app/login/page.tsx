import Link from "next/link"; import { AuthMessage,AuthPage } from "@/components/AuthPage"; import { loginAction,resendVerificationAction } from "../auth-actions";
export default async function LoginPage({searchParams}:{searchParams:Promise<{error?:string;notice?:string}>}){const q=await searchParams;return <AuthPage title="科研用户登录" description="登录后可进入个人中心。" footer={<><Link href="/register">注册账号</Link><span> · </span><Link href="/forgot-password">忘记密码</Link></>}><AuthMessage error={q.error} notice={q.notice}/><form action={loginAction} className="auth-form"><label>邮箱<input name="email" type="email" required autoComplete="email"/></label><label>密码<input name="password" type="password" required autoComplete="current-password"/></label><button type="submit">登录</button></form><details className="auth-resend"><summary>没有收到验证邮件？</summary><form action={resendVerificationAction} className="auth-form"><label>注册邮箱<input name="email" type="email" required/></label><button type="submit">重新发送</button></form></details></AuthPage>}
