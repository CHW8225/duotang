import Link from "next/link"; import { AuthMessage,AuthPage } from "@/components/AuthPage"; import { registerAction } from "../auth-actions";
export default async function RegisterPage({searchParams}:{searchParams:Promise<{error?:string}>}){const q=await searchParams;return <AuthPage title="注册科研用户" description="使用邮箱创建账号，验证后即可登录。" footer={<span>已有账号？ <Link href="/login">立即登录</Link></span>}><AuthMessage error={q.error}/><form action={registerAction} className="auth-form"><label>邮箱<input name="email" type="email" required autoComplete="email"/></label><label>密码<input name="password" type="password" required minLength={12} autoComplete="new-password"/></label><small>至少12个字符</small><button type="submit">注册并发送验证邮件</button></form></AuthPage>}
